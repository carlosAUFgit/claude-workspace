# claude-workspace

Workspace limpo. Sem projetos ativos no momento.

## Histórico

O conteúdo anterior foi removido do estado atual, mas continua acessível no histórico
do git:

| Conteúdo | Último commit com o conteúdo |
| --- | --- |
| `qemu-cachyos/` — kit de setup e tuning QEMU/KVM para CachyOS | `05a82a7` |
| `windows-vm/Debloat-WindowsVM.ps1` — script de debloat de VM Windows | `05a82a7` |

Para recuperar qualquer um deles:

```sh
git checkout 05a82a7 -- qemu-cachyos
git checkout 05a82a7 -- windows-vm
```

Ou apenas inspecionar sem restaurar:

```sh
git show 05a82a7:qemu-cachyos/README.md
```
